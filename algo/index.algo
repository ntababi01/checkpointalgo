ALGORITHM LongestWordLength
VAR
nbc,nbw,v:INTEGER;
ch:string;
BEGIN
 nbc := 1;
 nbw :=0;
 v:=0;
 ch:='';
DO {
    read(c);
         nbc:=nbc+1;
 IF (c in['o','a','i','u','y','e'])THEN
   v:=v+1;
 END_IF
 IF(c=' ')THEN
     nbw:=nbw+1;
 WHILE (c=('.'))
 END_WHILE            
    };
}
 